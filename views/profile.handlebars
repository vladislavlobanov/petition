<div class="bgPhoto edit"> </div>
<div class="bgOverlay edit"> </div>

<div class="frameHolder">

    <div class="insideframeHolder">
        <div class="frame">
            <form class="frameContainer" method="POST">

                <h1 class="regEl">TELL US ABOUT YOURESELF</h1>
                <div class="bannerRegister">#FREENAVALNY</div>

                <div class="regEl">
                    <div class="frameContainer center">
                        {{>error status=smthWrong}} 
                        <div>
                        <div class="orgInputs">
                            <label for="ageId">Age:</label>
                            <input
                                type="number"
                                id="ageId"
                                name="age"
                                min="18"
                                max="100"
                            /></div>

                        <div class="orgInputs">
                            <label for="cityId">City:</label>
                            <input type="text" id="cityId" name="city" /></div>

                        <div class="orgInputs" style="padding-bottom: 0;">
                            <label for="homepageId">Homepage:</label>

                            <input
                                type="text"
                                pattern="https?://.+"
                                id="homapageId"
                                name="homepage"
                                placeholder="http://example.com"
                            />

                        </div>
</div>
                        <input
                            type="hidden"
                            name="_csrf"
                            value="{{csrfToken}}"
                        />
                        <div class="wrapper"><button>CONTINUE</button></div>
                                                <div><a style="font-weight: bold;" href="/logout">LOG
                                OUT</a></div>

                    </div>
                </div>
            </form>
        </div></div>

</div>